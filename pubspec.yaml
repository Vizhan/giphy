name: flutter_giphy
description: A new Flutter application.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^6.1.1
  http: ^0.12.2
  dartz: ^0.9.2
  equatable: ^1.2.5
  get_it: ^5.0.1
  transparent_image: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_driver:
    sdk: flutter
  test: ^1.15.7
  mockito: ^4.1.3

flutter:
  uses-material-design: true